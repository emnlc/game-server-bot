services:
  ramen-games:
    build: .
    container_name: ramen-games
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - .:/app
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "5000:5000"
    working_dir: /app
